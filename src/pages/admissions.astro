---
import { Icon } from 'astro-icon/components';
import Hero from '../components/Hero.astro';
import MainLayout from '../layouts/MainLayout.astro';
import HousingApplication from '../components/HousingApplication.astro';
import { site } from '../data/site';
---

<MainLayout title="Admissions" description="Apply for a bed in a New Beginnings sober living house for men or women in Pittsburgh, or call 412-628-0403.">
  <Hero
    variant="split"
    height="viewport"
    titleId="admissions-title"
    eyebrow="Admissions"
    title="Apply for Recovery Housing"
    lead="Fill this out and we’ll call you back. Or skip it and call. Either way works, and you don’t need a referral."
    image={{ src: '/images/admissions-hero.webp', alt: 'A sunlit living room sofa with cushions beside open windows', width: 1467, height: 2200, position: 'center 62%', positionMobile: 'center 35%' }}
  >
    <Fragment slot="actions">
      <a class="button button--light" href="#inquiry">Start the Application<Icon name="lucide:arrow-down" aria-hidden="true" /></a>
      <a class="button button--outline-light" href={site.phoneHref}><Icon name="lucide:phone" aria-hidden="true" />Call {site.phone}</a>
    </Fragment>
  </Hero>

  <HousingApplication />
</MainLayout>

<style>
  /* Absolute so the portrait photo's aspect ratio cannot set the hero height; the copy column does. */

  @media (max-width: 900px) {
  }

  @media (max-width: 760px) {
  }
</style>
